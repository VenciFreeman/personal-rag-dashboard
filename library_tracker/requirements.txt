# Core web runtime dependencies.
# Setup script installs these into the monorepo top-level .venv used by web and worker scripts.
# First-time setup is usually done via the repository root setup_workspace.bat.
fastapi
uvicorn
jinja2
Pillow
openai>=1.0.0
argon2-cffi
